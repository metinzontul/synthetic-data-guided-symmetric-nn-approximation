# Scripts

- `current/`: primary scripts supporting the current manuscript.
- `supporting/`: auxiliary validation and earlier empirical implementations retained for traceability.
- `legacy/`: older plotting variants retained for provenance; these are not the primary source for the current manuscript numbering.

See `../docs/SCRIPT_MAP.md` for the exact mapping to manuscript figures and tables.
